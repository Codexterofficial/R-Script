#1. Click on help on 4th window

#2. Use question mark e.g.
?mean
?plot

#3. Use help.search e.g.,
help.search("mean")
help.search("matrix")

#4. To get an example of the function
example(lm)

#Library and packages
#What is packages?
#How we download a package?
#1. install button on 4th window
#2. mannually

#4. For know about the package of function use "find" e.g., 
find("mean")
find("anova")
find("ggplot")

#For calling a package we use "library" function
#for remove a package to use we use "detach" function


#Some shortcut key in R
#"rm" used for remove a inuse variable or function e.g.,
rm(data)
rm(list=ls()) #is used for clear environment
#for shortcutkey type Alt+Shift+K

#Commenting in R
#Use # for comment a single line
#For comment more line at once:- Select those lines and press Ctrl+Shift+R
#to remove commenting again press the same
