#Sampling
#Simple Random Sampling without replacement
Population<-LETTERS[1:15]
sample(Population,size=10)

#Simple Random Sampling with replacement
sample(Population,size = 10,replace = TRUE)
